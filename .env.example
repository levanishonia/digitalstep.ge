DATABASE_URL=
AUTH_SECRET=
OPENAI_API_KEY=
# Optional shared model override. Defaults to gpt-5-mini.
# Retained for compatibility and used when a workload-specific override is unset.
OPENAI_MODEL=
# Optional workload-specific model overrides. Each falls back to OPENAI_MODEL.
# AI Business Assistant
OPENAI_CHAT_MODEL=
# Post Generator and Content Ideas
OPENAI_CONTENT_MODEL=
# Marketing Planner and Business Analysis
OPENAI_ANALYSIS_MODEL=
